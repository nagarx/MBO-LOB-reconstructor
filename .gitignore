# Rust build artifacts
/target/
**/*.rs.bk

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test data (not included in repo)
/data/
*.dbn
*.dbn.zst

# Benchmark results
/criterion/

# Coverage
*.profraw
*.profdata
/coverage/

# AI assistant working files
CLAUDE.md
RULE.md

# Stale documentation copies (canonical version in feature-extractor docs/)
full-data-pipeline.md


# Nested git worktrees (candidate branches, v4 custody). These are full
# checkouts; without this rule a `git add -A` would commit an entire second
# working tree into this repo.
.worktrees/

# Machine-local Claude Code settings (never shared).
.claude/settings.local.json
